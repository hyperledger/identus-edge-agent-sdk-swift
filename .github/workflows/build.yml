---
name: Build

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]

jobs:
  lint:
    name: build
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build
        run: "swift build"
